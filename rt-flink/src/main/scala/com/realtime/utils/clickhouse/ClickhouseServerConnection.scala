package com.realtime.utils.clickhouse

import java.util.Properties

object ClickhouseServerConnection {

/*
  def writeMysql(config:String, df:DataFrame, tableName:String, saveMode:SaveMode) = {
    df.write.mode(SaveMode.Append)
      .option("batchsize", "100")
      .option("isolationLevel", "NONE") // 设置事务
      .option("numPartitions", "4") // 设置并发
      .option("driver", "ru.yandex.clickhouse.ClickHouseDriver")
      .jdbc("jdbc:clickhouse://localhost:8123/cluster_dwd", tableName, new Properties())
  }
*/

}
